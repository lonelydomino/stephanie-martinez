# Admin panel (What's New manager)

ADMIN_PASSWORD=choose-a-strong-password

# Required on Vercel so saves from /admin update the live site
GITHUB_TOKEN=github_pat_or_fine_grained_token_with_repo_contents_write
GITHUB_REPO=lonelydomino/stephanie-martinez
GITHUB_BRANCH=main

# Required on Vercel for cover image uploads (Storage → Blob in the dashboard)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_token_from_dashboard
